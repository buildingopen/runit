{$API_DOMAIN:api.runtime-ai.com} {
    reverse_proxy localhost:3001

    header {
        -Server
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
    }

    log {
        output file /var/log/caddy/runtime-api.log
        format filter {
            wrap json
            fields {
                request>uri query {
                    replace token REDACTED
                }
            }
        }
    }
}
