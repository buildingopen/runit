/**
 * Placeholder test for vitest
 *
 * Note: E2E tests are in *.spec.ts files and run with Playwright via `npm run test:e2e`
 */

import { describe, it, expect } from 'vitest';

describe('web app', () => {
  it('should have tests configured', () => {
    expect(true).toBe(true);
  });
});
